import styled from 'styled-components';

export const ErrorMessage = styled.span`
	font-size: 0.7rem;
	border-radius: 4px;
	border: solid 1px rgba(233, 67, 94, 0.8);
	background-color: rgba(233, 67, 94, 0.2);
	display: flex;
	gap: 0.4rem;
	padding: 0.1rem;
	color: #545060;
`;
